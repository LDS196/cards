import React from "react"

const Profile = () => {
    const logoutHandler = () => {}
    return (
        <div>
            Profile
            <button onClick={logoutHandler}></button>
        </div>
    )
}

export default Profile
